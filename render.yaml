services:
  - type: web
    name: community-hero-frontend
    runtime: static
    rootDir: community-hero-frontend
    buildCommand: npm install && GENERATE_SOURCEMAP=false npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
